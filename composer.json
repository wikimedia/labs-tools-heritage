{
    "require-dev": {
        "phpunit/phpunit": "4.8.*",
        "jakub-onderka/php-parallel-lint": "0.9.2"
    },
    "scripts": {
        "test": [
            "parallel-lint . --exclude vendor",
            "phpunit api/tests"
        ]
    },
    "require": {
        "Krinkle/intuition": "^0.2.3"
    }
}
