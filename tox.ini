[tox]
envlist = yaml,flake8,bashate,isort,jsonschema,py311,commit-message-validator
skipsdist=true

[testenv]
setenv =
    PYWIKIBOT_NO_USER_CONFIG = 1
# Since pywikibot fallback to the user home directory:
    HOME={envdir}
deps =
    -r{toxinidir}/requirements.txt
    -r{toxinidir}/requirements-dev.txt
commands = pytest {posargs}
allowlist_externals =
    bash
# .python-version must also list 3.9 since the CI image (tox-v3-pywikibot)
# only has tox installed under Python 3.9 via pyenv.
basepython = python3.11

[testenv:flake8]
deps =
    flake8==3.7
commands = flake8

[testenv:bashate]
deps = bashate==2.1.0
commands =
    bash -c "grep --recursive --binary-files=without-match \
        --files-with-match '^.!.*\(ba\)\?sh$' \
        --exclude-dir .tox \
        --exclude-dir .git \
        --exclude-dir .venv \
        --exclude-dir vendor \
        --exclude-dir node_modules \
        {toxinidir} | xargs bashate --error . --verbose --ignore=E006"

[testenv:isort]
deps = isort==5.13.2
commands =
    isort {posargs:--check-only --diff} --verbose \
        --skip .git --skip .tox --skip .venv --skip vendor --skip node_modules .

[testenv:yaml]
deps = yamllint
commands = yamllint {toxinidir}/

[testenv:jsonschema]
deps = check-jsonschema
allowlist_externals={toxinidir}/.jsonschema_cmd.sh
commands =
    {toxinidir}/.jsonschema_cmd.sh

[flake8]
exclude = .venv,.tox
ignore = E501,F841

[testenv:commit-message-validator]
deps = commit-message-validator
commands = commit-message-validator
